name: vision_text
description: Image + audio input, text only output

session:
  modalities: [text]
  temperature: 0.7
  input_audio_transcription:
    model: whisper-1
  turn_detection:
    type: server_vad
